#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>




int main() {
	//이차배열 포인터
	//int arr[2][3] = { { 1,2,3 }, { 4,5,6 } };  //첫번째는 생략 가능
	//int* p = arr;
	//for (int i = 0; i < 6; i++) {
	//	printf("%d  ",*p++);
	//}

	//두 학생의 국영수 점수를 배열에 저장
	//int sc[][3] = { {90,89,88},{87,52,59} };
	//int (*p)[3] = sc;  //() 괄호를 안하면 포인터가 배열이 있는게 됨 (포인터가 3개)
	//                   //현재는 포인터가 1개인데 3개의 배열짜리이다
	//int sum = 0; double avg;
	////printf("%p  %p",p,p+1);  //주소를 보면 12바이트 건너뜀
	//for (int i = 0; i < 2; i++); {
	//	sum = (*p)[0] + (*p)[1] + (*p)[2];
	//	avg = sum / 3.;
	//	printf("합계 :%d 평균:%.2f",sum, avg);
	//	p++;
	//}


































	return 0;
}